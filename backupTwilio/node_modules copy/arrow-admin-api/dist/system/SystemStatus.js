'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

/* eslint no-unused-expressions:0 */
/**
 * The Arrow server status.
 * @public
 */
var SystemStatus = function () {
	/**
  * Create a SystemStatus.
  *
  * @param {string} statusMessage - The status maessage.
  */
	function SystemStatus(statusMessage) {
		_classCallCheck(this, SystemStatus);

		this.statusMessage = statusMessage;

		if (!SystemStatus.validate(this)) {
			throw new Error('Invalid system status.');
		}
	}

	/**
  * The swagger schema for a SystemConfig.
  * @return {object} - The schema for a SystemConfig.
  */


	_createClass(SystemStatus, null, [{
		key: 'validate',


		/**
   * Validate that object is a SystemStatus.
   * @param {SystemStatus|object} obj - The object to validate.
   * @return {boolean} - true if the object is a valid SystemStatus.
   */
		value: function validate(obj) {
			var valid = true;
			SystemStatus.schema.required.forEach(function (prop) {
				if (!obj.hasOwnProperty(prop)) {
					valid = valid && false;
				} else {
					valid = valid && obj[prop] !== undefined;
				}
			});

			return valid;
		}
	}, {
		key: 'schema',
		get: function get() {
			return {
				title: 'System Status',
				type: 'object',
				properties: {
					statusMessage: {
						type: 'string'
					}
				},
				required: ['statusMessage']
			};
		}
	}]);

	return SystemStatus;
}();

exports = module.exports = SystemStatus;