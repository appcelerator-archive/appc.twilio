'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var APIProvider = require('../APIProvider'),
    Timeline = require('./Timeline'),
    TimelineEntry = require('./TimelineEntry'),
    BadRequestError = require('../errors/BadRequestError'),
    util = require('../util');

// jscs:disable jsDoc

var TimelineAPI = function (_APIProvider) {
	_inherits(TimelineAPI, _APIProvider);

	function TimelineAPI() {
		_classCallCheck(this, TimelineAPI);

		return _possibleConstructorReturn(this, (TimelineAPI.__proto__ || Object.getPrototypeOf(TimelineAPI)).apply(this, arguments));
	}

	_createClass(TimelineAPI, null, [{
		key: 'bind',
		value: function bind(controller) {
			var swagger = controller.ctx.swagger;

			swagger.schema('Timeline', Timeline.schema).schema('TimelineEntry', TimelineEntry.schema);

			swagger.get('/timeline/:id').operationId('getTimeline').tag('logging').summary('Gets an event timeline.').parameter({
				in: 'path',
				name: 'id',
				type: 'string',
				description: 'The event id.'
			}).response(200, 'OK', 'Timeline').response(400).response(404).response(500).action(function (req, res) {
				try {
					var eventId = util.getSafeReqId(req);
					if (!eventId) {
						throw new BadRequestError();
					}
					controller.getTimeline(eventId).then(function (data) {
						res.status(200).send(data);
					}).catch(function (ex) {
						// async block - errors have to be handled here
						APIProvider.handleError(ex, res, 400, 404, 500);
					});
				} catch (ex) {
					APIProvider.handleError(ex, res, 400, 404, 500);
				}
			});
		}
	}]);

	return TimelineAPI;
}(APIProvider);

exports = module.exports = TimelineAPI;