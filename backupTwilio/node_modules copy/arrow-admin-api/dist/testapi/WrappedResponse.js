'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

/**
* A wrapper containing the details for  an API response.
* @public
*/
var WrappedResponse = function () {
	/**
 * Create a WrappedResponse.
 *
 * @param {object} WrappedResponse - The values to initialize this WrappedResponse with.
 */
	function WrappedResponse(responseWrapper) {
		_classCallCheck(this, WrappedResponse);

		Object.assign(this, responseWrapper);

		if (!WrappedResponse.validate(this)) {
			throw new Error('Invalid wrapped response.');
		}
	}

	/**
 * The swagger schema for a WrappedResponse.
 * @return {object} - The schema for a WrappedResponse.
 */


	_createClass(WrappedResponse, null, [{
		key: 'validate',


		/**
  * Validate that object is a WrappedResponse.
  * @param {WrappedResponse|object} obj - The object to validate.
  * @return {boolean} - true if the object is a valid WrappedResponse.
  */
		value: function validate(obj) {
			var valid = true;
			this.schema.required.forEach(function (prop) {
				if (!obj.hasOwnProperty(prop)) {
					valid = false;
				}
			});

			return valid;
		}
	}, {
		key: 'schema',
		get: function get() {
			return {
				title: 'Response Wrapper',
				type: 'object',
				properties: {
					success: {
						type: 'boolean'
					},
					result: {
						type: 'object'
					},
					responseTime: {
						type: 'integer'
					},
					responseSize: {
						type: 'integer'
					},
					response: {
						type: 'object',
						properties: {
							headers: {
								type: 'object'
							},
							httpVersion: {
								type: 'string'
							},
							url: {
								type: 'string'
							},
							statusCode: {
								type: 'integer'
							}
						},
						required: ['statusCode']
					}
				},
				required: ['success', 'response', 'responseTime']
			};
		}
	}]);

	return WrappedResponse;
}();

exports = module.exports = WrappedResponse;