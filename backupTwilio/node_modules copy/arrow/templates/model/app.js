/**
 * NOTE: This file is simply for testing this model and will not
 * be used or packaged with the actual model when published.
 */
var Arrow = require('arrow'),
	server = new Arrow(),
	Model = require('./lib');

server.addModel(Model);

server.start();
