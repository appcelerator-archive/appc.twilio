var Arrow=require("arrow");
var api = Arrow.Block.extend({
   description: "this is an after block",
   name: "after",
   execute: function (req,resp,next) {
      var body = JSON.parse(resp.body);
      body.result.name = "bert";
      resp.body = body;
      next();
   }
});
module.exports = api;
