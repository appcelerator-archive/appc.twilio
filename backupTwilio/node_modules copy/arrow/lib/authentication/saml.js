// jscs:disable jsDoc

function _init(server) {
	var path = require('path'),
	Saml;
	var _p = path.resolve(process.cwd(), 'node_modules', 'appcelerator-saml');
	var _p_relative = '../../node_modules/appcelerator-saml/';
	try {
		Saml = require(_p);
	} catch (e) {
		Saml = require(_p_relative);
	}
	return new (Saml)(server);
}

module.exports = _init;
